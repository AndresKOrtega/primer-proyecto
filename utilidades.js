// exportar una función
export const saludar = (nombre) => {
  return `Hola, ${nombre}`
}

// exportar otra función
export const sumar = (a, b) => {
  return a + b
}

// export default — solo puede haber uno por archivo
const PI = 3.14159
export default PI